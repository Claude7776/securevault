#include "core.h"
#include <stdio.h>
#include <stdint.h>

void analyze_entropy(const uint8_t *data, size_t len) {
    (void)data; // Suppression de l'avertissement de variable non utilisée
    (void)len; // Suppression de l'avertissement de variable non utilisée
    printf("[Entropy] Analyse simulée de %zu bytes.\n", len);
}